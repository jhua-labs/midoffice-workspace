# 这个目录故意是空的。
#
# PRD 不预置在仓库里：如果它在这儿，Phase 1.1（扫描现状）和 1.2（生成项目宪法）
# 会先扫到它 —— 1.1 的扫描产物会索引这个文件，1.2 的宪法里会写进几条只可能
# 来自 PRD 的前瞻规则。而这两步的教学点恰恰是"只记录已经存在的东西"，
# 需求要到 Phase 2.1 才进场。
#
# 按 workshop 文档 1.3 那一步下载 rbac-prd.md 放到本目录即可。
