# ─────────────────────────────────────────────────────────────
# midoffice-workspace —— 规划 repo
#
# 跟踪「所有角色都要读」的规划产物：
#   requirements/   PRD 输入
#   _bmad-output/   PRD、架构、Epic、story spec、测试产物
#   docs/           既有系统的现状文档
#
# 不跟踪的两类：两个代码 repo（它们各自独立版本管理），
# 以及每台机器各自安装的工具文件（见第 2 节）。
# ─────────────────────────────────────────────────────────────


# ── 1. 代码 repo：各自独立，规划 repo 不跟踪 ─────────────────
/midoffice-api/
/midoffice-web/


# ── 2. 工具自己的安装文件：每人各装一份 ─────────────────────
# .kiro/  BMAD 的 skill 文件，每台机器自己安装。
# _bmad/  BMAD 配置。它同样由各人安装生成，而且里面写着
#         **安装者的名字**（user_name），提交上去等于把个人信息
#         带进仓库，所以这里也不跟踪。
#
# 代价：几个「大家必须取一致」的配置值不能靠 git 同步了，改为写在
# README 的「准备」一节，装完照着核对。至少这几项要一致：
#   project_name                  同一个值
#   communication_language        同一个值
#   document_output_language      同一个值
#   output_folder                 保持 {project-root} 相对写法
# （user_name 是各人自己的，本来就不该一致。）
.kiro/
_bmad/

# run_loop.py  Phase 5 批量跑 story 的驱动脚本，按文档从现场资产下载后
#              放在工作区根目录。和上面两项同理：属于「各人自己取一份的
#              工具」，不是这个起点项目的一部分，所以不跟踪。
run_loop.py


# ── 3. 机器专属产物：内含绝对路径，跨机器 clone 后无效 ───────
# 这一类最容易漏：提交时不报错，别人 clone 下来才发现路径全是你的
# 机器，而且依然不报错。
#
# 现状扫描报告（记录了本机的 project_root）
docs/project-scan-report.json

# UX 评审的过程记录（正文引用本机文件路径）
_bmad-output/planning-artifacts/**/.working/
_bmad-output/planning-artifacts/**/validation-report.md
_bmad-output/planning-artifacts/**/validation-report.html

# 浏览器验收的本地运行结果（记录了本机运行时与项目路径）
_bmad-output/implementation-artifacts/frontend-acceptance-results/
_bmad-output/**/test-results/
_bmad-output/**/playwright-report/


# ── 4. 状态跟踪表：要进 git，这里故意不排除它 ────────────────
# _bmad-output/implementation-artifacts/sprint-status.yaml 是全组共享
# 的进度表，后面的开发循环靠它认进度，必须进 git——所以本文件里
# 没有它的规则，别「顺手」补上。
#
# 它也是唯一一份「所有人都会写」的文件，跨机器容易冲突，配一条约定：
#   谁跑完一个 story、谁改完立刻 push，把冲突窗口压到最小。
#
# 提交前顺手改一处 —— 改的是那个 YAML 文件的内容，跟本文件（.gitignore）
# 的写法无关：它里面的 story_location 字段存的是**本机的绝对路径**
# （形如 /Users/你的用户名/... ），换台机器就指不到东西了。把它改成
# 相对于工作区根目录的路径再提交。这个字段没有任何工具会去解析，
# 纯粹是元数据，改了不影响功能。


# ── 5. 构建产物与依赖 ───────────────────────────────────────
# 第 1 节按目录名排除了代码 repo；这里再按类型兜一层，防止有人把
# 代码 repo clone 成了别的目录名。
node_modules/
target/
dist/
*.class


# ── 6. 本地数据库文件 ───────────────────────────────────────
data/
*.db
*.mv.db
*.trace.db


# ── 7. 编辑器与系统垃圾 ─────────────────────────────────────
.DS_Store
*.log
.idea/
*.iml
.vscode/


# ─────────────────────────────────────────────────────────────
# 改完怎么自查（两条命令）
#   git status --short
#       → 不应出现 midoffice-api/、midoffice-web/、.kiro/、_bmad/，
#         以及第 3 节那些产物
#   git check-ignore -v <某个具体文件路径>
#       → 会告出是哪一行规则命中的；没有输出说明这个文件没被忽略
# ─────────────────────────────────────────────────────────────
